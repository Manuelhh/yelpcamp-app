<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>YelpCamp - Get Edit Campgound Form</title>
  </head>
  <body>
    <h1>Edit one campground form page</h1>

    <form
      action="/campgrounds/<%= campgroundToEdit._id%>?_method=PUT"
      method="POST"
    >
      <div>
        <label for="title">Title</label>
        <input
          type="text"
          id="title"
          name="title"
          value="<%= campgroundToEdit.title %> "
        />
      </div>

      <div>
        <label for="location">Location</label>
        <input
          type="text"
          id="location"
          name="location"
          value="<%= campgroundToEdit.location %> "
        />
      </div>

      <button type="submit">Edit campground</button>

      <div>
        <a href="/campgrounds/<%= campgroundToEdit._id%>"
          >back to see the details of this background</a
        >
      </div>
    </form>
  </body>
</html>
